This mimi projects is meant to create a simple data plot using stars(*)
The terminal will be used as an output port. 
The function draw_graph() expects an input of a list of integer numbers [9,10,4,7,0,1,5]
